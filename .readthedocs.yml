version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - cmake
    - doxygen
  jobs:
    pre_build:
      - cmake -S . -B docs/sphinx/_build -D CMAKE_PROJECT_INCLUDE=$PWD/cmake/docs.cmake
      - cmake --build docs/sphinx/_build --target doxygen-xml

sphinx:
  configuration: docs/sphinx

formats: all

python:
  install:
    - requirements: docs/requirements.txt
